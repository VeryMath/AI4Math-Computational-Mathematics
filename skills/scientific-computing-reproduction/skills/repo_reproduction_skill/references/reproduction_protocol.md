# Reproduction Protocol

Analyze before running. Prefer explicit README commands, examples, demos, main entrypoints, tests, scripts, then benchmarks. Save source, logs, outputs, reports, and checkpoints under `outputs/{run_id}/`. Every external command requires a timeout.
