# External Algorithm Search Protocol

Use public metadata sources first:

- arXiv API for papers and preprints.
- GitHub REST repository search for public implementations.
- Crossref REST metadata can be added for DOI-oriented publication search.

Candidates must include source, title, URL, summary, query, and ranking evidence. The active coding agent must not select or run external code without human confirmation.
