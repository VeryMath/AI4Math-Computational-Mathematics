# OpenCode Loading Notes

This repository exposes a shared Skill layer at:

```text
skills/invariant-computation-skill/SKILL.md
```

Use from the checkout by asking OpenCode to read `AGENTS.md`, `SKILL.md`, and the concrete Skill file. If OpenCode supports local Skill discovery in your setup, link or copy `skills/invariant-computation-skill/` into that Skill path and reload.

Do not duplicate workflow logic in `.opencode/`; update the shared Skill layer.

